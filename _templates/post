---
layout: {{ layout }}
title: {{ title }}
author:
modified:
comments: true
categories: {{ dir }}
excerpt:
tags: [{{ dir }}]
image:
  url:
  alt: {{ title }}
  title: {{ title }}
  feature:
---

{% include _toc.html %}

<img src="{{ page.image.url }}" alt="{{ page.image.alt }}" title="{{ page.image.title }}">

{% highlight bash %}
$
{% endhighlight %}
